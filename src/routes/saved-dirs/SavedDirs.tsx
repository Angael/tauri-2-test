import { TextInput } from "@mantine/core";
import Layout from "../../components/Layout";

const SavedDirs = () => {
  return (
    <Layout>
      <TextInput label="Directory Name" placeholder="Enter directory name" />
    </Layout>
  );
};

export default SavedDirs;
